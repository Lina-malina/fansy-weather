const refreshBtn = () => document.getElementById('refresh_btn');
const searchBtn = () => document.getElementById('search_btn');
const searchInput = () => document.getElementById('searchInput');
export { refreshBtn, searchBtn, searchInput };
